Does this source text support the claim? Be lenient — if the source contains the key facts
mentioned in the claim (numbers, names, zones, measurements), answer SUPPORTED even if the
wording differs.

CLAIM: "{claim}"

SOURCE (from "{cited_doc}", page {cited_page}):
"{source_text}"

Answer exactly one of: SUPPORTED / PARTIALLY_SUPPORTED / NOT_SUPPORTED
Then one sentence why.